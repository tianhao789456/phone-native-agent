# Mobile Agent Plugins

Plugins follow the acornix-style shape:

```python
config = {"label": "Example", "icon": "*"}

def run():
    ...
```

The current CLI only lists plugins. Executing arbitrary plugins will require an approval layer first.

